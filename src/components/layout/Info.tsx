import classes from "./Info.module.scss"

const Info = () => {
    return (
        <div className={`${classes.container} p-2 background`}>
            v0.1
        </div>
    )
}

export default Info